import UserForm from '../../components/form/UserForm';

const Input = () => {
    return (
        <div className="flex h-[100%] w-full flex-col place-items-center content-center items-center justify-center justify-items-center p-10 align-middle text-blue-300">
            <UserForm />
        </div>
    );
};

export default Input;
